#!/usr/bin/perl 
#
# feature handler for price regression task
#
package MLTask::Amazon::PriceRegression::Model;

use strict;
use warnings;
use Exporter;

use vars qw($VERSION @ISA @EXPORT);

$VERSION = 1.0;
@ISA = qw (Exporter);
# export nothing. avoid clash
@EXPORT = ();


1;
