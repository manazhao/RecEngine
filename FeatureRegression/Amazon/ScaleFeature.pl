#!/usr/bin/perl
#
# scale feature to the range [0,1]
#

use strict;
use warnings;

my %feat_range_map = ();

while(<>){
	my ($feat_id, @feats)  = split /\,/;
	foreach (@feats){
		my ($fid,$fval) = split /\:/;
		
	}

}
